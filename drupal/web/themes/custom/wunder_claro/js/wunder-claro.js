/**
 * @file
 * JS opcional del subtema Wunder Claro.
 */
(function (Drupal) {
  'use strict';

  Drupal.behaviors.wunderClaro = {
    attach(context) {
      // Punto de enganche para comportamientos del admin.
    },
  };
})(Drupal);
